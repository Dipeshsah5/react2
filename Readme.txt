Requirement:
1) nodejs must be installed in the system


1. Create React app by (create-react-app "app_name") using this command in terminal.
2. Override the public and src folder.
3. Then Start npm Server by (npm start) using this command in terminal.